{% extends 'base.html.twig' %}

{% block title %}New Todo Item{% endblock %}

{% block body %}
<div>
    <a href="{{ path('home') }}"><button>Back to Home Page</button></a>
</div>

<h3>New Todo Item</h3>

{{ form_start(form) }}
    <input type="hidden" name="add_todo_item_form[todoListId]" value="{{ id }}">
    {{ form_errors(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.save) }}
{{ form_end(form) }}

<style>
label {
    margin-right: 10px;

}
button[type="submit"] {
    margin-top: 10px;
}
</style>
{% endblock %}
